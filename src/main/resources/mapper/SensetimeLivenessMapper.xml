<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.payu.app.modules.mapper.SensetimeLivenessMapper">
    <resultMap id="BaseResultMap" type="cn.com.payu.app.modules.entity.SensetimeLiveness">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="cuser_id" jdbcType="INTEGER" property="cuserId"/>
        <result column="biz_no" jdbcType="VARCHAR" property="bizNo"/>
        <result column="biz_token" jdbcType="VARCHAR" property="bizToken"/>
        <result column="liveness_image" jdbcType="VARCHAR" property="livenessImage"/>
        <result column="liveness_face_image" jdbcType="VARCHAR" property="livenessFaceImage"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="del_flag" jdbcType="TINYINT" property="delFlag"/>
        <result column="created_date" jdbcType="TIMESTAMP" property="createdDate"/>
        <result column="created_by" jdbcType="INTEGER" property="createdBy"/>
        <result column="updated_date" jdbcType="TIMESTAMP" property="updatedDate"/>
        <result column="updated_by" jdbcType="INTEGER" property="updatedBy"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
        -->
        id, cuser_id, biz_no, biz_token, liveness_image, liveness_face_image, `status`, del_flag, created_date, created_by, updated_date,
        updated_by
    </sql>

    <select id="selectByBizNo" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_sensetime_liveness
        where del_flag = 0
        and biz_no = #{bizNo}
    </select>

    <select id="selectByBizToken" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_sensetime_liveness
        where del_flag = 0
        and biz_token = #{bizToken}
    </select>

    <select id="selectLastLivenessByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_sensetime_liveness
        where del_flag = 0
        and cuser_id = #{userId}
        and status = 1
        order by id desc
        limit 1
    </select>

    <insert id="saveOrUpdate" useGeneratedKeys="true" keyProperty="id" parameterType="cn.com.payu.app.modules.entity.SensetimeLiveness">
        INSERT INTO t_sensetime_liveness (id, cuser_id, biz_no, biz_token, liveness_image, liveness_face_image, `status`, del_flag, created_date)
        VALUES (#{id}, #{cuserId}, #{bizNo}, #{bizToken}, #{livenessImage}, #{livenessFaceImage}, #{status}, 0, sysdate()) ON DUPLICATE KEY
        UPDATE
            liveness_image = #{livenessImage,jdbcType=TINYINT},
            liveness_face_image = #{livenessFaceImage,jdbcType=TINYINT},
            updated_date = sysdate()
    </insert>
</mapper>